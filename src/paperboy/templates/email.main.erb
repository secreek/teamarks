Subject: <%= templet.subject %>
MIME-Version: 1.0
Content-Type: multipart/alternative; boundary="<%= templet.marker%>"

This is a message in multipart MIME format.  Your mail client should not
be displaying this. Consider upgrading your mail client to view this
message correctly.
--<%= templet.marker%><% templet.mail_parts.each do |mail_part| %>
<%= mail_part.to_s %>
--<%= templet.marker%><% end %>
